<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"> <!-- Replace "com.example.elibpl" with your desired package name -->

    <!-- Rest of the code -->

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
<application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Elibpl"
        tools:targetApi="31">
        <activity
            android:name=".account_navigation"
            android:exported="false"
            android:label="@string/title_activity_account_navigation"
            android:theme="@style/Theme.Elibpl.NoActionBar" />
        <activity
            android:name=".admin_homepage"
            android:exported="false"
            android:label="@string/title_activity_admin_homepage"
            android:theme="@style/Theme.Elibpl.NoActionBar" />
        <activity
            android:name=".admin_login_page"
            android:exported="false" />
        <activity
            android:name=".RegisterActivity"
            android:exported="false" />

        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>