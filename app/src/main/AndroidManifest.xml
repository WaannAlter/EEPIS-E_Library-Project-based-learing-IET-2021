<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Elibpl"
        tools:targetApi="31">
        <activity
            android:name=".accound_navigation"
            android:exported="false"
            android:label="@string/title_activity_accound_navigation"
            android:theme="@style/Theme.Elibpl.NoActionBar" />
        <activity
            android:name=".admin_homepage"
            android:exported="false"
            android:label="@string/title_activity_admin_homepage"
            android:theme="@style/Theme.Elibpl.NoActionBar" />
        <activity
            android:name=".admin_login_page"
            android:exported="false" />
        <activity
            android:name=".register"
            android:exported="false" />
        <activity
            android:name=".main_admin"
            android:exported="false"
            android:label="@string/title_activity_main_admin"
            android:theme="@style/Theme.Elibpl.NoActionBar" />
        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>